# Copy to .streamlit/secrets.toml for local testing against a cloud DB, or
# paste this key into the "Secrets" box in Streamlit Community Cloud's app
# settings when deploying. .streamlit/secrets.toml itself is gitignored —
# never commit real credentials.

DATABASE_URL = "postgresql://user:password@host:port/dbname"
